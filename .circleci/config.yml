version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.15
        auth:
          username: aidarionov
          password: $DOCKERHUB_PASSWORD
    steps:
      - checkout
      - run:
          name: Linting files
          command: |
            tsc
            eslint . --ext .js,.jsx,.ts,.tsx
